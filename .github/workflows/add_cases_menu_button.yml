name: Legacy - disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled. Current bot configuration is managed by patch_cases_button_now.yml."
